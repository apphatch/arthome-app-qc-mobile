import { DefaultTheme, Colors } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: Colors.deepPurple600,
    secondary: '#414757',
    error: '#f13a59',
  },
};
