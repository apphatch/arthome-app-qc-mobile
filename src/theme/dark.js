import {DarkTheme} from 'react-native-paper';

export const theme = {
  ...DarkTheme,
  colors: {
    ...DarkTheme.colors,
    primary: '#1ba1f2',
    secondary: '#414757',
    error: '#f13a59',
  },
};
